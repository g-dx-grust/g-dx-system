{
    "name": "@g-dx/application",
    "version": "0.1.0",
    "private": true,
    "main": "src/index.ts",
    "types": "src/index.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json --noEmit",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@g-dx/domain": "workspace:*"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}
